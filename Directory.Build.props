<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <Import Project="Directory.Build.props.tests"
          Condition="$(MSBuildProjectName.EndsWith('.Tests'))" />
</Project>
